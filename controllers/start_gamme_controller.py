"""Module controller du menu start gamme"""

class StartGameController:
    """Menu controller"""

    def __call__(self):
        print("Dans le controleur de start gamme")
        return
    
    """Reste à faire"""
    # lancement du tournoi
    #   - création des matchs (joueur 1 contre joueur 2)
    #   - Enregistrement des données en tuple (list_of_matchs)
    #   - Création du round 1
    #   - Enregistrement des données dans (current_round)
    #   - Validation des résultats des matchs envoie vers le controller (end_round_controller)
